# Frontier Save Schema 3

Schema 3 preserves the state introduced by the visual-survival milestone: persistent world age, mining mode, partial block damage, fractional micro-material harvest, inventory/hotbar, live world drops, ordinary region edits and separate microvoxel-region occupancy masks. Legacy schema 1/2 saves remain readable and migrate forward on the next save.
